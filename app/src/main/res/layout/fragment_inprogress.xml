<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".scenes.home.fragment.InprogressFragment">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rcvListVideoInProgress"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:orientation="vertical"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:listitem="@layout/item_file_in_progress" />

    <include
        android:id="@+id/sflLoadingData"
        layout="@layout/layout_loading_data"
        android:visibility="gone" />

    <include
        android:id="@+id/llVideoNotFound"
        layout="@layout/layout_no_data_video_found"
        android:visibility="gone" />

</androidx.constraintlayout.widget.ConstraintLayout>